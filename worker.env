# 僚机配置文件
# 请根据实际情况修改以下配置

# ============= 网络配置 =============
# 主机内网IP（必须与主机配置一致）
MASTER_HOST_IP=192.168.1.100

# ============= 工作节点配置 (直连主节点，无需大量本地资源) =============
# 工作节点唯一ID (每台工作节点必须不同: 1, 2, 3, ..., 9)
WORKER_ID=1

# ============= 应用配置 =============
# 数据库连接（直连主节点）
POSTGRES_HOST=postgres
POSTGRES_DB=taskdb
POSTGRES_PORT=5432
POSTGRES_USER=worker
POSTGRES_PASSWORD=password

# Redis连接（直连主节点）
REDIS_HOST=redis
REDIS_IMG=redis:7.2.5-alpine3.19

# MinIO连接（直连主节点）
MINIO_HOST=minio
MINIO_IMG=minio/minio:RELEASE.2024-05-28T17-19-04Z
MINIO_ROOT_USER=admin
MINIO_ROOT_PASS=password
MINIO_BUCKET=workflow

# ============= 性能调优 =============
# 证明参数
SEGMENT_SIZE=21
RISC0_KECCAK_PO2=17
REDIS_TTL=57600
SNARK_TIMEOUT=180

# 日志配置
RUST_LOG=info
